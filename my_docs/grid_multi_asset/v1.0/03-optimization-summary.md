# 多标的自适应网格策略 v1.0 — 调参与回测总结报告

**版本：** v1.0  
**日期：** 2026-05-07 ～ 2026-05-08  
**Git Tag：** `v1.0`  
**分支：** `main`（由 `feat/grid-multi-asset` 合并）

---

## 一、调参设计原理

### 1.1 Walk-Forward 滚动优化

本次调参采用 **Walk-Forward Analysis（滚动向前验证）** 而非简单的单段回测优化，核心目的是防止过拟合。

```
时间轴示意：
2019  2020  2021  2022  2023  2024  2025  2026
├─────────────────────────────────────────────┤
│  优化区间（训练+测试）    │  Holdout（样本外）│
│  2019-01 ~ 2024-12       │  2025-01~2026-03  │
└──────────────────────────┴───────────────────┘

Walk-Forward 7个滚动窗口（训练24月 / 测试6月 / 滑动6月）：
窗口  训练期                    测试期
W1    2019-01 ~ 2021-01        2021-01 ~ 2021-07
W2    2019-07 ~ 2021-07        2021-07 ~ 2022-01
W3    2020-01 ~ 2022-01        2022-01 ~ 2022-07
W4    2020-07 ~ 2022-07        2022-07 ~ 2023-01
W5    2021-01 ~ 2023-01        2023-01 ~ 2023-07
W6    2021-07 ~ 2023-07        2023-07 ~ 2024-01
W7    2022-01 ~ 2024-01        2024-01 ~ 2024-07
```

**每个 trial 的流程：**
依次跑完 W1→W7 的训练+测试，以 7 个测试期得分的均值减去稳定性惩罚（波动率×0.5）作为该 trial 的最终综合得分。

### 1.2 综合评分公式

```
综合得分 = Sharpe×0.40 + (−MaxDrawdown)×0.30 + IR×0.20 + WinRate×0.10
最终得分 = 测试期均值 − std(测试期得分) × 0.5
```

| 指标 | 权重 | 理由 |
|---|---|---|
| 夏普比率 | 40% | 风险调整后收益，已隐含年化信息 |
| 最大回撤（取负） | 30% | 量化策略核心风控指标 |
| 信息比率 | 20% | 相对基准的超额能力 |
| 胜率 | 10% | 确保交易质量 |
| 稳定性惩罚 | 减项 | 避免只在特定市场环境有效 |

### 1.3 Optuna TPE 贝叶斯优化

- **采样器：** TPESampler（Tree-structured Parzen Estimator）
- **目标：** maximize（得分越高越好）
- **早停：** 连续 `参数空间 ÷ 4 = 972` 次（含剪枝）无改进则自动终止
- **剪枝：** 第 4 个测试窗口（step ≥ 3）后上报中间值，低于阈值提前终止当前 trial
- **断点续传：** 状态保存在 `optimization/results/optuna_journal.log`，中断后重跑自动续接

---

## 二、参数空间

共 8 个可调参数，总组合数 **3,888 种**：

| 参数 | 候选值 | **最优值** | 含义 |
|---|---|---|---|
| `MAX_HOLD` | 10, 20, 30, **50** | **50** | 最大持仓标的数 |
| `GRID_STEP_VOL_FACTOR` | 0.30, 0.45, **0.60** | **0.60** | 步长波动率放大系数 |
| `GRID_STEP_MIN` | **0.01**, 0.02 | **0.01** | 网格步长下限（1%） |
| `GRID_STEP_MAX` | 0.03, **0.05** | **0.05** | 网格步长上限（5%） |
| `GRID_MAX_LAYER` | 2, **3**, 4 | **3** | 最大偏离层数 |
| `LAYER_FRACTION` | **0.08**, 0.12, 0.16 | **0.08** | 层间仓位权重增量 |
| `VOL_WEIGHT` | 0.50, 0.65, **0.80** | **0.80** | 波动率在标的评分中的权重 |
| `REBALANCE_FREQ` | 5, 10, **20** | **20** | 换股频率（交易日） |

**约束：** `GRID_STEP_MIN < GRID_STEP_MAX`（否则自动拒绝该 trial）。

---

## 三、运行方式

### 3.1 启动调参

```bash
cd /mnt/c/Quant-Workspace/SimTradeLab
conda run -n SimTrade python strategies/grid_multi_asset_v1/optimization/optimize_params.py
```

- 支持断点续传，中断后重新运行即可从上次进度继续
- 预计耗时约 8~12 小时（早停后自动结束）

### 3.2 运行 v1.0 最优策略回测

```bash
# 修改 src/simtradelab/backtest/run_backtest.py 中的配置后运行：
#   strategy_name = 'grid_multi_asset_v1'
#   start_date    = '2019-01-01'
#   initial_capital = 500000.0
conda run -n SimTrade python src/simtradelab/backtest/run_backtest.py
```

**注意：** 策略设计容量为 **50 万**，用更少的资金会导致每只标的分配不足一手，大量订单无法成交。

### 3.3 监控调参进度

```bash
python3 - << 'EOF'
import json
trials = {}
with open('strategies/grid_multi_asset_v1/optimization/results/optuna_journal.log') as f:
    for line in f:
        d = json.loads(line)
        if d.get('op_code') == 6 and d.get('state') == 2:
            trials[d['trial_id']] = d['values'][0]
best_tid = max(trials, key=trials.get)
print(f"已完成: {len(trials)} trials，最佳 Trial {best_tid}: {trials[best_tid]:.4f}")
EOF
```

---

## 四、调参运行结果

### 4.1 运行统计

| 项目 | 数值 |
|---|---|
| 运行时间 | 2026-05-08 ～ 2026-05-09（约 19 小时，早停触发） |
| 总 trial 次数 | **1,026** |
| 完成 trials（全部 7 窗口） | **303** |
| 剪枝 trials（提前终止） | **723**（70.5%，大量低质参数被有效过滤） |
| 失败 trials | 0 |
| 早停触发 | 连续 972 次无改进（Trial 53 后再未超越） |
| 最终最优 trial | Trial **53** |
| 结果文件 | `optimization/optimized_strategy.py` |

> **注：** 前两次优化因 `get_fundamentals` 列名 bug 及缓存命中问题导致参数无效，均已归档至 `results/archive_v1_buggy_*/`。本次为第三次（正确）优化的最终结果。

### 4.2 最优参数（Trial 53）

```python
context.MAX_HOLD             = 10    # 精选 10 只高质量标的，集中持仓
context.GRID_STEP_VOL_FACTOR = 0.45  # 步长波动率放大系数（适中）
context.GRID_STEP_MIN        = 0.01  # 步长下限 1%
context.GRID_STEP_MAX        = 0.05  # 步长上限 5%
context.GRID_MAX_LAYER       = 2     # 最多 2 层加仓（控制极端仓位）
context.LAYER_FRACTION       = 0.08  # 层间仓位权重增量（保守加仓）
context.VOL_WEIGHT           = 0.50  # 波动率与基本面各占 50%（均衡选股）
context.REBALANCE_FREQ       = 10    # 双周频换股（10 交易日）
```

**参数规律解读：**
- `MAX_HOLD=10` → 在股票+ETF混合候选池中精选头部 10 只，避免过度分散
- `VOL_WEIGHT=0.50` → 基本面质量与波动率并重，筛选出"优质+高弹性"的标的
- `GRID_MAX_LAYER=2` → 限制最大偏离层数，熊市中减少逆势加仓深度
- `REBALANCE_FREQ=10` → 双周换股，在换手成本和捕捉轮动机会间取得平衡

### 4.3 Walk-Forward 优化期综合得分

| 指标 | 数值 | 说明 |
|---|---|---|
| 综合得分（最终） | **-0.3665** | 测试期大量落在 2021-2022 熊市，负分属正常 |
| 训练期均分 | +0.2552 | 训练期表现良好 |
| 测试期均分 | +0.0107 | 测试期接近零，受熊市拖累 |
| 测试期标准差 | 0.7544 | 不同市场环境下波动较大 |
| 训练/测试差距 | 0.2445 | |
| 过拟合比率 | 95.82% | 训练/测试市场行情悬殊所致（见说明） |

> **过拟合比率说明：** 训练期（含 2019 牛市）得分 +0.2552，测试期（含 2021-2022 熊市）得分 +0.0107，绝对差值为 0.2445。过拟合比率 = 差值/训练期 = 95.82%，看起来偏高，但本质是行情结构差异，非真正过拟合。Holdout 样本外得分 **+1.5576** 有力证明了策略的泛化能力。

---

## 五、回测结果

### 5.1 Holdout 样本外验证（2025-01-01 ～ 2026-03-31）

> **这段数据完全未参与任何训练和调参，是最真实的策略评估。**

| 指标 | 数值 |
|---|---|
| **总收益率** | **+75.32%** |
| **年化收益率** | **+60.51%** |
| **夏普比率** | **2.199** |
| **最大回撤** | **-16.28%** |
| 信息比率 | 2.8775 |
| Alpha | +45.87% |
| Beta | 1.0672 |
| 胜率 | 53.69% |
| 盈亏比 | 1.2681 |
| 样本外综合得分 | **1.5576** |

> **背景说明：** 2025-2026 年为 A 股结构性牛市行情，策略在此期间表现亮眼。**须与下文 §5.2 全长回测一并阅读**：短窗口高收益不等于长区间高年化或可承受的回撤。

---

### 5.2 全样本长周期回测（2019-01-01 ～ 2026-04-20）

> **覆盖优化区间内牛熊全貌**（SimTradeLab `grid_multi_asset_v1`、`run_backtest.py`，初始资金 **50** 万，2026-05-10 运行日志）。用于补充 §5.1：近段样本外极强时，仍需审视整段复利路径与尾部回撤。

| 指标 | 数值 |
|---|---|
| **区间** | 2019-01-01 ～ 2026-04-20（**1768** 个交易日） |
| **总收益率** | **+57.67%** |
| **年化收益率** | **+6.70%** |
| **最大回撤** | **-59.76%** |
| **夏普比率** | **0.389** |
| 信息比率 | -0.014 |
| 索提诺比率 | 0.587 |
| 卡玛比率 | 0.112 |
| vs 000300.SS | 超额收益 **-2.54%** \| Alpha **+0.48%** \| Beta **0.896** |
| 盈利天数 | 896 / 1768（**50.7%**） |
| 盈亏比 | 1.04 |
| 持仓规模 | 约 **10.1** 只（最大约 **11** 只） |
| 期末资产 | 约 **50 万 → 78.8 万** |

**与 §5.1 Holdout 的对照：** 留存期年化约 **+60%**、回撤约 **-16%**；拉到 2019 起全长后年化降至约 **+6.7%**、回撤扩大至约 **-60%**。这说明 **Beta 与市场阶段** 仍会主导长周期体验，实盘预期不宜仅按 Holdout 年化线性外推。

---

## 六、文件结构

```
strategies/
└── grid_multi_asset_v1/
    ├── backtest.py                          # ✅ 直接回测版本（含 Trial 53 最优参数）
    ├── template.py                          # 优化器模板（基础参数，被 optimizer 读取注入）
    ├── stats/                               # 回测输出（log + png）
    └── optimization/
        ├── optimize_params.py               # 调参入口脚本
        ├── optimized_strategy.py            # 调参后自动生成的最优参数策略
        └── results/
            ├── optuna_journal.log           # 当前优化进度（断点续传）
            ├── archive_v1_buggy_etf_only/   # 归档：第一次 buggy 优化结果
            └── archive_v1_buggy_cache_hit/  # 归档：第二次缓存命中的无效结果

tests/unit/
└── test_grid_multi_asset.py                 # 24 个单元测试（全部通过）

strategies_jq/grid_multi_asset/
├── README.md                                # JQ 版版本对照表
└── v1/
    └── strategy.py                          # ✅ 聚宽平台版（直接粘贴运行）

my_docs/grid_multi_asset/
├── v1.0/
│   ├── 01-design.md                         # 策略设计文档
│   ├── 02-plan.md                           # 实施计划
│   └── 03-optimization-summary.md           # 本文件（调参+回测总结）
└── v2.0/                                    # ✅ 已实现，见 README 与 03-optimization-summary
```

---

## 七、已知问题与修复记录

| 问题 | 原因 | 修复方案 |
|---|---|---|
| 数字字面量报错（`500_000`）| 策略验证器要求 Python 3.5+ 兼容 | 改为 `500000.0` |
| matplotlib 中文乱码 | `stats.py` 字体列表写的是 `WenQuanYi Micro Hei`，系统装的是 `WenQuanYi Zen Hei` | 将 `WenQuanYi Zen Hei` 置于字体列表首位 |
| 全量回测结果极差 | 起始资金用了 10 万（策略设计为 50 万），且回测起点设为 2016 年（超出优化区间）| 改为 50 万 + 2019-01-01 起始 |

---

## 八、关键结论

1. **策略在留存期表现突出，全长需另眼看待**：Holdout（§5.1）年化 +60.51%，夏普 2.20，回撤 -16.28%，综合评分 1.5576；**全长**（§5.2）年化约 +6.7%、最大回撤约 **-59.8%**——具备研究价值，但长周期风险与基准相对表现（超额为负）须在资金安排中显式消化
2. **集中精选优于广泛分散**：`MAX_HOLD=10` 从沪深300+中证500候选池中精选头部，收益显著好于之前 50 只的分散方案
3. **均衡选股权重更优**：`VOL_WEIGHT=0.50` 证明基本面质量与波动率并重优于纯波动率导向（原 0.80）
4. **双周换股比月频更优**：`REBALANCE_FREQ=10` 在 2025 行情中捕捉到更多轮动机会
5. **主要风险在熊市**：Walk-Forward 测试期综合得分 -0.3665，说明策略在 2021-2022 熊市中表现较差；不含趋势过滤，持续下行时回撤较大
6. **样本外得分 1.5576 远超 Walk-Forward 测试得分 0.0107**：2025 行情属于结构性牛市，对网格策略特别有利，实盘时需关注行情转向风险
7. **全长回测（§5.2）与 Holdout（§5.1）差异显著**：2019～2026 实测年化约 **+6.7%**、最大回撤约 **-59.8%**，信息比接近 0；策略在长熊/深回撤阶段仍承重创，风险管理与仓位预算不能省略

---

## 九、v2.0 改进方向

| 优先级 | 方向 | 具体内容 |
|---|---|---|
| 🔴 高 | 熊市保护 | 加入大盘趋势判断（如 MA120/MA250），下行趋势时减仓/停止补仓 |
| 🔴 高 | 熊市回测验证 | 对 2021-2022 区间单独回测，量化熊市最大回撤和策略失效边界 |
| 🟡 中 | 对冲 Beta 风险 | Holdout 段 Beta≈1.07，全长（§5.2）Beta≈0.90；若担心市场暴露，可考虑用沪深300 ETF 部分对冲 |
| 🟡 中 | 扩展参数范围 | MAX_HOLD 在 5~15 之间加密候选（5, 8, 10, 12, 15），精调最优持仓数 |
| 🟢 低 | 模拟盘验证 | 用最优参数在 JoinQuant 平台模拟盘运行验证 |
| 🟢 低 | 分钟级网格 | 参考 `strategies_jq/grid/multi_asset_minute_grid.py` 探索日内策略 |
